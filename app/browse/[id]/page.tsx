import { notFound } from "next/navigation";
import { listings } from "@/lib/data";
import { Button } from "@/components/layout";

export default function AccountDetail({ params }: { params: { id: string } }) { const listing = listings.find(x => x.id === params.id); if (!listing) notFound(); return <main className="mx-auto max-w-6xl px-5 py-14"><div className="grid gap-10 lg:grid-cols-[1.1fr_.9fr]"><div><div className="flex h-[420px] items-center justify-center rounded-3xl border border-line bg-gradient-to-br from-zinc-800 via-zinc-900 to-electric/10 text-9xl">{listing.image}</div><div className="mt-10"><p className="text-xs font-bold uppercase tracking-[.2em] text-electric">About this listing</p><p className="mt-4 max-w-2xl leading-8 text-zinc-400">{listing.description}</p><h3 className="mt-10 text-lg font-semibold">Included items</h3><div className="mt-4 flex flex-wrap gap-2">{listing.items.map(item => <span className="rounded-full border border-line bg-panel px-4 py-2 text-sm text-zinc-300" key={item}>{item}</span>)}</div></div></div><div><p className="text-sm text-zinc-500">{listing.game}</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">{listing.title}</h1><div className="mt-7 grid grid-cols-2 gap-3"><div className="rounded-xl border border-line bg-panel p-4"><p className="text-xs text-zinc-500">Rank</p><p className="mt-2 font-semibold">{listing.rank}</p></div><div className="rounded-xl border border-line bg-panel p-4"><p className="text-xs text-zinc-500">Level</p><p className="mt-2 font-semibold">{listing.level}</p></div></div><div className="mt-8 border-y border-line py-7"><p className="text-xs text-zinc-500">Price</p><p className="mt-1 text-4xl font-semibold">${listing.price} <span className="text-base font-normal text-electric">USDC</span></p></div><div className="flex items-center gap-3 py-7"><div className="grid h-11 w-11 place-items-center rounded-full bg-electric font-bold text-ink">{listing.seller[0].toUpperCase()}</div><div><p className="font-medium">{listing.seller}</p><p className="text-sm text-zinc-500">★ {listing.rating} · {listing.reviews} reviews</p></div></div><Button onClick={() => alert("Purchase flow coming soon — this is a demo.")} className="w-full">Buy now · ${listing.price} USDC</Button><div className="mt-4 rounded-xl border border-electric/20 bg-electric/5 p-4 text-sm"><p className="font-semibold text-electric">▣ Protected by Lootly Escrow</p><p className="mt-2 leading-6 text-zinc-400">Your funds stay protected until you confirm access. Secure payments are coming in the next phase.</p></div></div></div></main>; }

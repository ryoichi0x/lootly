"use client";
import { useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";
import Link from "next/link";
import { getSupabaseBrowserClient } from "@/lib/supabase/client";
import { Button } from "@/components/layout";
export default function Login() { const router = useRouter(); const params = useSearchParams(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit(e: React.FormEvent) { e.preventDefault(); setLoading(true); setError(""); const { error: authError } = await getSupabaseBrowserClient().auth.signInWithPassword({ email, password }); if (authError) { setError(authError.message); setLoading(false); } else router.push(params.get("next") || "/dashboard"); } return <AuthForm title="Welcome back." subtitle="Log in to manage your Lootly account." submit={submit} email={email} setEmail={setEmail} password={password} setPassword={setPassword} error={error} loading={loading} footer={<p>New to Lootly? <Link href="/signup" className="text-electric">Create an account</Link></p>}/>; }
export function AuthForm({ title, subtitle, submit, email, setEmail, password, setPassword, error, loading, footer }: any) { return <main className="mx-auto max-w-md px-5 py-20"><p className="text-xs font-bold uppercase tracking-[.2em] text-electric">Lootly account</p><h1 className="mt-3 text-4xl font-semibold">{title}</h1><p className="mt-3 text-zinc-400">{subtitle}</p><form onSubmit={submit} className="mt-10 space-y-5 rounded-2xl border border-line bg-panel p-6 sm:p-8"><label>Email<input required type="email" value={email} onChange={e => setEmail(e.target.value)} className="field"/></label><label>Password<input required minLength={6} type="password" value={password} onChange={e => setPassword(e.target.value)} className="field"/></label>{error && <p className="text-sm text-red-300">{error}</p>}<Button disabled={loading} className="w-full">{loading ? "Please wait..." : title.includes("Create") ? "Create account" : "Log in"}</Button><div className="text-center text-sm text-zinc-500">{footer}</div></form></main>; }

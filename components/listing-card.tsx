import Link from "next/link";
import { Listing } from "@/lib/data";

export function ListingCard({ listing }: { listing: Listing }) { return <Link href={`/browse/${listing.id}`} className="group overflow-hidden rounded-2xl border border-line bg-panel transition hover:-translate-y-1 hover:border-zinc-600"><div className="flex h-44 items-center justify-center bg-gradient-to-br from-zinc-800 via-zinc-900 to-electric/10 text-6xl grayscale transition group-hover:grayscale-0">{listing.image}</div><div className="p-5"><div className="mb-3 flex items-start justify-between gap-3"><div><p className="text-xs text-zinc-500">{listing.game}</p><h3 className="mt-1 font-semibold leading-snug">{listing.title}</h3></div><span className="whitespace-nowrap text-lg font-bold">${listing.price}<small className="text-xs font-normal text-zinc-500"> USDC</small></span></div><div className="flex gap-2 text-xs text-zinc-400"><span className="rounded bg-white/5 px-2 py-1">{listing.rank}</span><span className="rounded bg-white/5 px-2 py-1">Lvl {listing.level}</span></div><div className="mt-5 flex justify-between border-t border-line pt-4 text-xs text-zinc-500"><span>★ {listing.rating} ({listing.reviews})</span><span className="text-electric">View listing →</span></div></div></Link>; }
export function SectionHeading({ eyebrow, title, link }: { eyebrow: string; title: string; link?: string }) { return <div className="mb-8 flex items-end justify-between gap-4"><div><p className="mb-2 text-xs font-bold uppercase tracking-[.2em] text-electric">{eyebrow}</p><h2 className="text-3xl font-semibold tracking-tight sm:text-4xl">{title}</h2></div>{link && <Link href={link} className="hidden text-sm text-zinc-400 hover:text-electric sm:block">View all →</Link>}</div>; }

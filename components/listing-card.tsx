import type { Listing } from "@/lib/data";
import Link from "next/link";
export function ListingCard({ listing }: { listing: Listing }) { const image = listing.image_url ? <img src={listing.image_url} alt="" className="h-full w-full object-cover"/> : <span className="text-6xl">{["🎯", "⚡", "◈", "⌁", "✦", "◒"][listing.game.length % 6]}</span>; return <Link href={`/browse/${listing.id}`} className="group overflow-hidden rounded-2xl border border-line bg-panel transition hover:-translate-y-1 hover:border-zinc-600"><div className="flex h-44 items-center justify-center bg-gradient-to-br from-zinc-800 via-zinc-900 to-electric/10 grayscale transition group-hover:grayscale-0">{image}</div><div className="p-5"><div className="mb-3 flex items-start justify-between gap-3"><div><p className="text-xs text-zinc-500">{listing.game}</p><h3 className="mt-1 font-semibold leading-snug">{listing.title}</h3></div><span className="whitespace-nowrap text-lg font-bold">${listing.price_usdc}<small className="text-xs font-normal text-zinc-500"> USDC</small></span></div><div className="flex gap-2 text-xs text-zinc-400"><span className="rounded bg-white/5 px-2 py-1">{listing.rank || "Unranked"}</span><span className="rounded bg-white/5 px-2 py-1">Lvl {listing.level || "—"}</span></div><div className="mt-5 border-t border-line pt-4 text-xs text-zinc-500"><span className="text-electric">View listing →</span></div></div></Link>; }
